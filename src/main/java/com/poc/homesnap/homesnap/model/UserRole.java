package com.poc.homesnap.homesnap.model;
 
public enum UserRole {
    FREE,      // Free users can generate up to 3 house plans
    REGISTERED, // Registered users can generate up to 5 house plans
    SUBSCRIBER  // Subscribers have unlimited house plan generation
} 